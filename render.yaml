services:
  # Spring Boot 3 Java 21 Backend Web Service
  - type: web
    name: opsflow-api
    env: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: starter
    healthCheckPath: /api/v1/health
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: PORT
        value: 8080
      - key: JWT_SECRET
        generateValue: true
      - key: DB_HOST
        fromDatabase:
          name: opsflow-postgres
          property: host
      - key: DB_PORT
        fromDatabase:
          name: opsflow-postgres
          property: port
      - key: DB_NAME
        fromDatabase:
          name: opsflow-postgres
          property: database
      - key: DB_USER
        fromDatabase:
          name: opsflow-postgres
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: opsflow-postgres
          property: password
      - key: REDIS_HOST
        fromService:
          type: redis
          name: opsflow-redis
          property: host
      - key: REDIS_PORT
        fromService:
          type: redis
          name: opsflow-redis
          property: port
      - key: REDIS_PASSWORD
        fromService:
          type: redis
          name: opsflow-redis
          property: password

  # Redis 7 In-Memory Cache and Pub/Sub Broker
  - type: redis
    name: opsflow-redis
    plan: starter
    maxmemoryPolicy: allkeys-lru

databases:
  # Managed PostgreSQL 16 Database
  - name: opsflow-postgres
    plan: starter
    postgresMajorVersion: 16
    databaseName: opsflow
    user: opsflow
